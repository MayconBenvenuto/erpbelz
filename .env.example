## === Supabase ===
# URL do projeto Supabase (obrigatório)
NEXT_PUBLIC_SUPABASE_URL=
# Chave pública (anon) do Supabase para o cliente (obrigatório)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Chave de Service Role (NÃO exponha no cliente; use apenas no servidor)
SUPABASE_SERVICE_ROLE_KEY=

## === Segurança ===
# Chave JWT (mínimo 32 caracteres aleatórios)
JWT_SECRET=
# Custo do bcrypt (12 recomendado)
BCRYPT_ROUNDS=12
# Tempo de sessão em ms (padrão: 24h)
SESSION_TIMEOUT=86400000

## === CORS ===
# Origens permitidas, separadas por vírgula (sem espaços)
# Ex.: http://localhost:3000,https://seudominio.com
CORS_ORIGINS=http://localhost:3000

## === Rate Limiting ===
# Janela em ms (15 minutos = 900000)
RATE_LIMIT_WINDOW=900000
# Máximo de requisições por IP na janela
RATE_LIMIT_MAX_REQUESTS=100

## === SMTP / E-mail ===
# Host/porta do servidor SMTP
SMTP_HOST=
SMTP_PORT=587
# Credenciais SMTP (se requer autenticação)
SMTP_USER=
SMTP_PASS=
# TLS/SSL: defina true para SMTPS (porta 465 normalmente)
SMTP_SECURE=false
# Debug SMTP (apenas dev): logs detalhados e verificação
SMTP_DEBUG=false
# TLS SNI opcional (defina se o servidor exigir)
SMTP_TLS_SERVERNAME=
# Pode ser false em dev para ignorar cert autoassinado
SMTP_TLS_REJECT_UNAUTHORIZED=true
# Endereço remetente padrão e nome
EMAIL_FROM=
EMAIL_FROM_NAME=CRM Belz
# Responder para (Reply-To) opcional
EMAIL_REPLY_TO=
# Override de destino (DEV somente, para testes de disparo)
EMAIL_OVERRIDE_TO=

# Fallback de e-mail via Resend (opcional, usado se SMTP indisponível)
RESEND_API_KEY=

## === Apps ===
# Base pública do app (usada no cliente e em alguns e-mails)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# URL canônica do app (priorizada em e-mails e links externos)
ERP_APP_URL=http://localhost:3000

## === Integrações ===
# Chave para provedores de CNPJ (ex.: CNPJA)
CNPJA_API_KEY=

## === API/Runtime ===
# Ambiente de execução (dev, production). Em produção, configure via plataforma.
NODE_ENV=development

# Observação: Se possuir chave de terceiros (ex.: CNPJA), preencha CNPJA_API_KEY para melhorar a confiabilidade da validação de CNPJ.
